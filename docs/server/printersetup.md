---
id: printersetup
title: 🖨️ Setup Printer
sidebar_position: 4
---

export const Highlight = ({children, color}) => (
  <span
    style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
    {children}
  </span>
);

# Setup Printer Server

# Setup Printer Client